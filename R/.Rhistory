names(stdEffs) <- params
}
return(unlist(stdEffs))
}
ExtractStdTmb(out, params = c("beta"), reNames = NULL)
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w", "block1"))
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
stdEffs <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
names(stdEffs) <- reNames
}
return(unlist(stdEffs))
}
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w", "block1"))
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
stdEffs <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
}
names(stdEffs) <- reNames
return(unlist(stdEffs))
}
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w", "block1"))
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
stdEffs <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
}
names(stdEffs) <- reNames
print(stdEffs)
return(unlist(stdEffs))
}
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w", "block1"))
ExtractStdTmb(out, params = c("beta"), reNames = NULL)
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
tmp <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
}
out <- unlist(tmp)
names(out) <- reNames
return(out)
}
ExtractStdTmb(out, params = c("beta"), reNames = NULL)
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w", "block1"))
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
tmp <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
}
out <- unlist(tmp)
names(out) <- reNames
return(cbind(out))
}
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w", "block1"))
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
tmp <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
}
out <- unlist(tmp)
names(out) <- reNames
return(cbind("Std. Error" =out))
}
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w", "block1"))
ExtractStdTmb(out, params = c("beta"), reNames = NULL)
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
tmp <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
}
reNames <- ifelse(is.null(reNames), params, reNames)
out <- unlist(tmp)
names(out) <- reNames
return(cbind("Std. Error" =out))
}
ExtractStdTmb(out, params = c("beta"), reNames = NULL)
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
tmp <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
if(is.null(reNames))
names(std) <- params
}
out <- unlist(tmp)
if(!is.null(reNames))
names(out) <- reNames
return(cbind("Std. Error" =out))
}
ExtractStdTmb(out, params = c("beta"), reNames = NULL)
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
tmp <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
if(is.null(reNames))
names(tmp) <- params
}
out <- unlist(tmp)
if(!is.null(reNames))
names(out) <- reNames
return(cbind("Std. Error" =out))
}
ExtractStdTmb(out, params = c("beta"), reNames = NULL)
summary(sdreporttmbObj)[, "Std. Error"]
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
tmp <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
if(is.null(reNames))
names(tmp) <- idx
}
out <- unlist(tmp)
if(!is.null(reNames))
names(out) <- reNames
return(cbind("Std. Error" =out))
}
ExtractStdTmb(out, params = c("beta"), reNames = NULL)
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
tmp <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
}
out <- unlist(tmp)
if(!is.null(reNames))
names(out) <- reNames
return(cbind("Std. Error" = out))
}
ExtractStdTmb(out, params = c("beta"), reNames = NULL)
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
tmp <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
}
out <- unlist(tmp)
if(!is.null(reNames))
names(out) <- reNames
return(cbind("Std. Error" = out))
}
ExtractStdTmb(out, params = c("beta"), reNames = NULL)
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj,
params = NULL,
reNames = NULL){
#load dllID
dyn.load(TMB::dynlib(tmbObj$dllID))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
tmp <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
}
out <- unlist(tmp)
if(!is.null(reNames)){
stopifnot(length(out)==length(reNames))
names(out) <- reNames
}
return(cbind("Std. Error" = out))
}
ExtractStdTmb(out, params = c("beta"), reNames = NULL)
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w"))
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w", "block1"))
#' Function to extract  standard error parameters from a list containing TMB::MakeADFun and nlminb Objects
#'
#' @param tmbObj A list that contains the TMB::MakeADFun and nlminb tmbObjects.
#' @param params Parameter names to extract. If NULL, all parameters will be extracted.
#' @param reNames A vector of names to rename parameters. If NULL, the original TMB names will be retained.
#' @return A matrix of chosen parameters with their standard error
#' @author Jemay Salomon
## @examples
#'
#'@export
ExtractStdTmb <- function(tmbObj, path = NULL,
params = NULL,
reNames = NULL){
if (!is.null(path)) {
dll <- file.path(path, tmbObj$dllID)
} else {
dll <- tmbObj$dllID
}
# Load the DLL using dyn.load
dyn.load(TMB::dynlib(dll))
#range check
if(!is.list(tmbObj)) stop("tmbOj must be a list")
#require tmb packages and make summary
requireNamespace(package="TMB")
sdreporttmbObj <- TMB::sdreport(tmbObj$f)
if (is.null(params)) {
stdEffs <- summary(sdreporttmbObj)[, "Std. Error"]
} else {
if (!is.character(params)) stop("The 'params' argument must be a character vector.")
tmp <- lapply(params, function(std) {
idx <- which(rownames(summary(sdreporttmbObj)) == std)
if (length(idx) == 0) stop(paste("std effect '", std, "' not found in summary."))
summary(sdreporttmbObj)[idx, "Std. Error"] })
}
out <- unlist(tmp)
if(!is.null(reNames)){
stopifnot(length(out)==length(reNames))
names(out) <- reNames
}
return(cbind("Std. Error" = out))
}
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w", "block1"), path = NULL)
setwd("/home/jemay/Documents/PhD_Jemay/tmbExtract/R/")
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w", "block1"), path = NULL)
ExtractStdTmb(out, params = c("beta"), reNames = c("mu-w", "block1"), path = "/home/jemay/Documents/PhD_Jemay/phdmobidiv/results/")
